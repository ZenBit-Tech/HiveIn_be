export enum Status {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  EXPIRED = 'expired',
}

export enum tablesToSearch {
  USER = 'userFreelancer',
  FREELANCER = 'freelancer',
}
